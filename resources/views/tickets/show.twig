{% extends 'templates/app.twig' %}

{% block content %}
    <div class="box box-primary">
        <div class="box-body">
            <div class="row" style="padding-left: 20px">
                {% include 'tickets/show_fields.twig' %}
                <a href="{{ path_for('tickets.index') }}" class="btn btn-default">Voltar</a>
            </div>
        </div>
    </div>
{% endblock %}